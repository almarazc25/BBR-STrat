# PEACHY ENGINE — dependencies
# Install with:  pip install -r requirements.txt

# --- Schwab data path (default) ---
# Pinned <3 because schwabdev v3 changed its constructor signature and
# breaks our client. v2.x is stable.
schwabdev>=2.4.0,<3.0.0

# --- Yahoo Finance fallback path (no auth, ~15 min delayed) ---
yfinance>=0.2.40
scipy>=1.11.0  # for BSM greeks on the Yahoo path

# --- Local .env loader (no-op if not present) ---
python-dotenv>=1.0.0

# Standard library covers the rest (urllib for Pushover, zoneinfo, etc.).
